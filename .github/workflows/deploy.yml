name: Deploy to DreamHost

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via rsync over SSH
        uses: easingthemes/ssh-deploy@v5.1.0
        env:
          SSH_PRIVATE_KEY: ${{ secrets.DREAMHOST_SSH_KEY }}
          REMOTE_HOST: iad1-shared-b8-28.dreamhost.com
          REMOTE_USER: dh_ih56c7
          TARGET: /home/dh_ih56c7/thomas-graham.me/
          SOURCE: "./"
          ARGS: "-rltgoDzvO --delete"
          EXCLUDE: "/.git/, /.github/, /.gitignore, /.DS_Store"
